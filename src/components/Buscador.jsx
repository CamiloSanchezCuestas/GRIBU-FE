import React from "react";
import { ScrollView, Text, View, Image, StyleSheet } from "react-native";

const Buscador = () => {
    return (
        <Text>
            Este debería ser el buscador por etiquetas
        </Text>
    );
}

const styles = StyleSheet.create({
    title: {
        fontSize: 20,
        marginTop: 50
    }
});

export default Buscador